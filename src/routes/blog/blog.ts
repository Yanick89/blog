import { blogs } from "../../table";

export function load (params) {
    // const post = blogs.find(post => post.id === params.id);
    // return{
    //     post
    // }
    console.log('tester');
    
}